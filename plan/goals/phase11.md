# Phase 11 `/goal` prompt (for gpt-5.5 / Codex)

Paste everything below as the `/goal`:

---

/goal Complete Phase 11 ("Topic decomposition and facets") of `plan/build_plan.md`. The binding spec is Amendment 2026-07-04 (d) — read it first; it records the pilot round-2 mega-topic finding, the library-docs research, and exactly what this phase changes. Build on the shipped Phases 0–10.

Desired end state: the clustering-space UMAP follows the library's own clustering guidance (`min_dist=0.0`, exposed in config); a topic can be decomposed by a `focus` cluster run that re-clusters only its members (the answer to a 45%-of-the-graph mega-topic that global tuning provably cannot split); topics and evidence reads support `facetBy` breakdowns over any record field or metadata key; and the README teaches the decomposition workflow — all verified by a green suite including the planted-structure gates.

Deliverables:

1. Expose `cluster.space.minDist` in graph config, DEFAULT `0.0` (validated: float ≥ 0), passed to the clustering-space UMAP only — the LAYOUT UMAP keeps its own `minDist` (default 0.1) untouched. Rationale (UMAP clustering docs): densely packed points give HDBSCAN cleaner separations; 0.1 is a visualization default that was silently feeding the clusterer. The effective value must appear in the cluster run's echoed params. The planted-structure test gate (ARI ≥ 0.8) must still pass WITH the new default — if it does not, stop and report rather than lowering the gate.
2. Focus reclustering: `POST /api/graphs/:gid/views/:vid/cluster` accepts optional `focus: {clusterRunId?, clusterId}`. Resolution at POST time: `clusterRunId` defaults to the view's `default_cluster_run_id` (409 naming the cluster endpoint if none); it must be a succeeded cluster run of this view (404 otherwise); `clusterId` must exist in that run's `cluster_summaries` (404 with the valid ids in the message otherwise). Execution: population = the focus cluster's member records (from `cluster_memberships`) ∩ the view scope ∩ embedding coverage — then the normal pipeline (UMAP reduce with the graph/override config, HDBSCAN, canonical ids, memberships, summaries, representatives). `input_refs_json` gains `{"focusClusterRunId": ..., "focusClusterId": ...}` for lineage. Focus runs are INSPECTION runs: `setDefault` is forced `false` — an explicit `setDefault: true` with `focus` returns 422 with a message explaining drill-downs cannot become the view default (their population is a subset; the view's artifact/layout cover the full scope). The full drill workflow must work through existing override params and needs NO new read endpoints: label the focus run via `POST .../label {clusterRunId: <focusRun>, setDefault: false}`, inspect via `GET .../topics?clusterRunId=<focusRun>` and `.../topics/:tid/records?clusterRunId=...` (mismatch warnings appearing on override reads are correct, informational behavior). Document this exact workflow in the README with a curl sequence.
3. `facetBy` on reads: `GET .../topics?facetBy=<field>` and the evidence endpoint (optional `"facetBy"` in the request body, applied to `surprising_topics`/`new_topics`/`vanishing_topics`/`rising_topics`/`compare_periods` topic entries and to `topic_evidence`). Allowed fields: `sourceType`, `sourceName`, `product`, `sku`, `sentiment`, `rating`, `tags` (each tag counted), or `metadata.<key>` (top-level metadata keys only). Unknown field → 422 listing the allowed forms. Each topic entry gains `facets: {"<value>": count, ...}` computed over that topic's member records via SQL (memberships × records), with null/absent bucketed as `"(none)"`, capped at the top 20 values plus an `"(other)": n` rollup. The ARTIFACT shape is unchanged — facets are API-read features, not artifact payload.
4. README: a "Decomposing A Large Topic" section — when one topic swallows the graph (the round-2 mega-topic), the focus workflow (curl sequence: focus cluster → label with clusterRunId → read with overrides), plus the documented escape hatches for global granularity (`clusterSelectionMethod: "leaf"` balanced by `clusterSelectionEpsilon`, per HDBSCAN docs' guidance on EOM's large-cluster signature — both already in config); document `facetBy` with an example; add "device/platform relay notifications" (printer confirmations, platform payout/chargeback notices) to the Pre-Upload Filtering checklist (round-2 survivor category).

Verified by — run all of these; do not claim completion from belief:

- `.venv/bin/pytest` green, including at minimum: `minDist` validated (negative/non-number → 422), passed through to the clustering-space UMAP (assert via echoed params and a unit-level check of the reducer construction), layout UMAP unaffected; the planted-structure ARI ≥ 0.8 gate passing with the new 0.0 default; focus end-to-end on planted data — focus a ground-truth topic's cluster, assert the sub-run's population equals that cluster's membership exactly, memberships/summaries/representatives persisted with canonical ids, `input_refs` lineage recorded, view defaults untouched; focus + `setDefault: true` → 422; focus with unknown clusterId → 404 listing valid ids; focus with no default cluster run → 409; the full drill workflow through override params (focus → label via clusterRunId → topics?clusterRunId returns labeled sub-topics); `facetBy` counts matching independently computed tallies for a record field AND a `metadata.<key>` (use `groundTruthTopicId`), `"(none)"` bucketing, top-20 + `"(other)"` capping (unit-testable with a synthetic high-cardinality field), unknown facet → 422, applied on topics AND at least two evidence recipes including `topic_evidence`; artifact byte-shape unchanged (existing artifact tests unmodified except where minDist default shifts coordinates).
- `scripts/quality_eval.py` present and runnable with the new default (the reviewer will run the real-key ARI comparison — current bar: 0.860 with min_dist 0.1; report expectations honestly if synthetic results shift).
- `ruff check .`, `npx knip`, `npm run build`, `npm run check` all green; suite runtime reported (budget ~4.5 minutes).

While preserving: all Phase 0–10 tests green (updated only where the minDist default legitimately shifts clustering-space geometry); artifact shape and UI untouched (no frontend changes this phase); `plan/` untouched; no new dependencies; CI makes no network calls; layout defaults unchanged; the evidence endpoint still creates no runs (facetBy is computed in-request over persisted memberships).

Between iterations: run pytest and ruff after each meaningful change and let failures pick the next action; keep a running list of decisions or deviations from `plan/build_plan.md` and include it in the final summary.

If blocked — the ARI gate fails under `min_dist=0.0` (report the measured ARI and stop; do not lower the gate or quietly revert the default), focus-run population semantics collide with a view state this goal doesn't anticipate, or facet SQL performance is pathological — stop and report the exact blocker, what was attempted, the evidence gathered, and the decision needed. Do not add facets to the artifact, allow focus runs to become view defaults, or invent facet fields beyond the listed forms.
